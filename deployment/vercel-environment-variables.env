# Vercel Environment Variables

# Copy these to Vercel Dashboard → Project Settings → Environment Variables
# Add them for all environments: Production, Preview, and Development

# =============================================================================
# SERVER-SIDE ENVIRONMENT VARIABLES
# =============================================================================

# Main API URL (Laravel Backend)
URL=https://api.neelayurvedics.in/api/admin

# Storage URL (for uploaded images)
storageURL=https://api.neelayurvedics.in

# Admin URL (for theme images)
adminURL=https://api.neelayurvedics.in/admin/

# =============================================================================
# CLIENT-SIDE ENVIRONMENT VARIABLES (Accessible in Browser)
# These MUST start with NEXT_PUBLIC_ prefix
# =============================================================================

NEXT_PUBLIC_STORAGE_URL=https://api.neelayurvedics.in
NEXT_PUBLIC_ADMIN_URL=https://api.neelayurvedics.in/admin/
NEXT_PUBLIC_API_URL=https://api.neelayurvedics.in/api/admin

# =============================================================================
# ENVIRONMENT
# =============================================================================

NODE_ENV=production

# =============================================================================
# OPTIONAL: VERCEL ANALYTICS (if you enable them)
# =============================================================================

# NEXT_PUBLIC_VERCEL_ANALYTICS_ID=your_analytics_id_here

# =============================================================================
# INSTRUCTIONS
# =============================================================================

# 1. Go to https://vercel.com/dashboard
# 2. Select your project: fastkart-admin
# 3. Go to Settings → Environment Variables
# 4. Add each variable above with its value
# 5. Select which environments to apply to (Production, Preview, Development)
# 6. Click "Save"
# 7. Redeploy your application for changes to take effect

# =============================================================================
# IMPORTANT NOTES
# =============================================================================

# - Server-side variables (without NEXT_PUBLIC_) are NOT accessible in browser
# - Client-side variables (with NEXT_PUBLIC_) ARE accessible in browser
# - Never expose sensitive data in NEXT_PUBLIC_ variables
# - After adding/updating variables, you must redeploy
# - Each environment (prod/preview/dev) can have different values

# =============================================================================
# FOR LOCAL DEVELOPMENT
# =============================================================================

# Create a .env.local file in the root directory with these variables
# The .env.local file should NOT be committed to Git
# It's already in .gitignore
